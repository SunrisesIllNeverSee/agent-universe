# Local dev — enables write endpoints without admin key
CIVITAE_DEV_MODE=1

# Production only (Railway) — not needed for local dev
# CIVITAE_ADMIN_KEY=
# KASSA_JWT_SECRET=
# KASSA_JWT_SECRET_PREV=    # only set during secret rotation window
# RESEND_API_KEY=
# OPERATOR_EMAIL=
# ALLOWED_ORIGIN=
